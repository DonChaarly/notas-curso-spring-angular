package notas;

public class Proceso {

	/*
	 * Se configuro la conexion a la base de datos en el archivo application.properties
	 * Se creo la clase Cliente, implementando la interface Serializable por buena practica
	 * Se creo la inteface IClienteDao a la cual se le extendio de la interface CrudRepository
	 * Se creo la Interface IClienteService y la implementacion ClienteService Impl
	 * Se creo la clase ClienteRestController
	 * Se creo el archivo import.sql para agregar algunos datos de prueba (spring detecta el archivo import y ejecuta las sentencias automaticamente)
	 * Se implemento el manejo de errores dentro del controlador 
	 * 		Implementando el envio de objetos ResponseEntity<?>
	 * Se impolementarion validaciones a la clase de entidad
	 * Se implementaron validaciones al controlador
	 * Se modifico la interface implementada en interface DAO
	 * Se agrego un metodo pageable en interface Service
	 * Se creo una interface para el manejo de archivo IUploadFileService
	 * Se creo la implementacion de esta interface
	 * Se creo la clase entity Region
	 * Se agregaron metodos a la clase DaoCliente, ClaseSERviceEntity y Controlador
	 * 
	 */
	
	
}
